angular.module('merchello').controller('Merchello.Sales.Dialogs.EditLineItem',
    ['$scope',
        function ($scope) {

            function init() {

                $scope.loaded = true;
            }

            init();
        }]);
