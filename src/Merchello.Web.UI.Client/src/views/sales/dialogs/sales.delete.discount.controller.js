angular.module('merchello').controller('Merchello.Sales.Dialogs.DeleteDiscount',
    ['$scope',
        function ($scope) {

            function init() {

                $scope.loaded = true;
            }

            init();
        }]);
