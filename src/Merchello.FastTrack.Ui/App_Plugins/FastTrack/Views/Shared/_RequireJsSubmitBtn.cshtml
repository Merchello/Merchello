@model Merchello.Web.Models.Ui.IRequireJs
@if (Model.RequireJs)
{<input type="submit" class="btn btn-default pull-right mui-requirejs" value="Save" />}
else
{<input type="submit" class="btn btn-default pull-right" value="Save" />}