@inherits UmbracoViewPage<Merchello.Implementation.Models.CheckoutAddressModel>
@using Merchello.Implementation.Controllers

<h2>Shipping Address</h2>
@using (Html.BeginUmbracoForm<DefaultCheckoutAddressController>("SaveBillingAddress", new { area = "Merchello" }, new { data_muistage = "BillingAddress" }))
{
    <div class="control-group">
        @Html.LabelFor(x => x.Name)
        @Html.ValidationMessageFor(x => x.Name)
        @Html.TextBoxFor(x => x.Name)
    </div>

    <div class="control-group">
        @Html.LabelFor(x => x.Email)
        @Html.ValidationMessageFor(x => x.Email)
        @Html.TextBoxFor(x => x.Email)
    </div>

    <div class="control-group">
        <label>Company name (Optional)</label>
        @Html.TextBoxFor(x => x.Organization)
    </div>
    <div class="control-group">
        <label>Address line 1 *</label>
        @Html.ValidationMessageFor(x => x.Address1)
        @Html.TextBoxFor(x => x.Address1)
    </div>
    <div class="control-group">
        <label>Address line 2</label>
        @Html.TextBoxFor(x => x.Address2)
    </div>
    <div class="control-group">
        <label>Town / City *</label>
        @Html.ValidationMessageFor(x => x.Locality)
        @Html.TextBoxFor(x => x.Locality)
    </div>
    <div class="control-group">
        <label>Postal Code *</label>
        @Html.ValidationMessageFor(x => x.PostalCode)
        @Html.TextBoxFor(x => x.PostalCode)
    </div>


    @Html.HiddenFor(x => x.Key)
    @Html.HiddenFor(x => x.AddressType)
    @Html.AntiForgeryToken()
    <div class="buttons stage-buttons">
        <div class="buttons stage-buttons">
            <div class="row">
                <div class="col-md-4 col-md-offset-4 pull-right">
                    <input type="submit" class="btn-silver btn-full-width btn-next" value="Next" />
                </div>
            </div>
        </div>
    </div>
}